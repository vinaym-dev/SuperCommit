name: Super Commit on PR approval

on:
  pull_request_review:
    types: [submitted]

jobs:
  on_approval:
    if: >
      github.event.review.state == 'approved' &&
      startsWith(github.event.pull_request.head.ref, 'feature/')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Derive Jira key from branch
        id: vars
        shell: bash
        run: |
          BRANCH="${{ github.event.pull_request.head.ref }}"
          if [[ "$BRANCH" =~ ([A-Z]+-[0-9]+) ]]; then
            echo "key=${BASH_REMATCH[1]}" >> $GITHUB_OUTPUT
          else
            echo "key=" >> $GITHUB_OUTPUT
          fi

      # ✅ Step 1: Transition Build → Validate Test (existing logic, unchanged)
      - name: Transition Build → Validate Test (no Tempo log)
        if: ${{ steps.vars.outputs.key != '' }}
        env:
          COMMIT_MESSAGE: "${{ steps.vars.outputs.key }} STATUS:Validate Test LOG:0h COMMENT:Auto transition on PR approval"
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          TEMPO_TOKEN: ${{ secrets.TEMPO_TOKEN }}
          TEMPO_AUTHOR_ACCOUNT_ID: ${{ secrets.TEMPO_AUTHOR_ACCOUNT_ID }}
          DRY_RUN: "false"
        run: node scripts/supercommit/index.js

      # ✅ Step 2: Update Ready = Yes only for auto-created PRs (Build → Validate Test)
      - name: Auto-update Ready=Yes for Auto PRs
        if: >
          github.event.review.state == 'approved' &&
          contains(github.event.pull_request.title, 'Auto PR (Build → Validate Test)') &&
          steps.vars.outputs.key != ''
        env:
          COMMIT_MESSAGE: "${{ steps.vars.outputs.key }} READY:Yes COMMENT:Auto Ready flip on PR approval"
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          TEMPO_TOKEN: ${{ secrets.TEMPO_TOKEN }}
          TEMPO_AUTHOR_ACCOUNT_ID: ${{ secrets.TEMPO_AUTHOR_ACCOUNT_ID }}
          DRY_RUN: "false"
        run: node scripts/supercommit/index.js